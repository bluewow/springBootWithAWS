{{>layout/header}}  <!--현재 머스테치 파일 기준으로 다른파일을 가져온다-->

<h1>스프링 부트로 시작하는 웹 서비스Ver.2</h1>
<div class="col-md-12">
    <!--로그인 기능영역-->
    <div class="row">
        <div class="col-md-6">
            <a href="/posts/save" role="button" class="btn btn-primary"> 글 등록</a>
            {{#userName}}   <!--머스테치는 if 문을 제공하지 않는다 only true/false-->
                Logged in as: <span id="user">{{userName}}</span>
                <a href="/logout" class="btn btn-info active" role="button">Logout</a>
                <!--Spring Security 에서 제공하는 기본 URL(개발자가 컨트롤러를 만들필요없다)-->
            {{/userName}}
            {{^userName}}   <!--해당값이 존재하지 않는 경우-->
                <a href="/oauth2/authorization/google" class="btn btn-success active" role="button">Google Login</a>
                <!--Spring Security 에서 제공하는 기본 URL(개발자가 컨트롤러를 만들필요없다)-->
            {{/userName}}
            {{^userName}}   <!--해당값이 존재하지 않는 경우-->
                <a href="/oauth2/authorization/naver" class="btn btn-success active" role="button">Naver Login</a>
                <!--Spring Security 에서 제공하는 기본 URL(개발자가 컨트롤러를 만들필요없다)-->
                <!--네이버 로그인 URL 은 properties 에 등록한 redirect-uri 값이 자동으로 등록된다-->
            {{/userName}}
        </div>
    </div>
    <br>
    <!--목록출력 영역-->
    <table class="table table-horizontal table-bordered">
        <thead class="thead-strong">
        <tr>
            <th>게시글번호</th>
            <th>제목</th>
            <th>작성자</th>
            <th>최종수정일</th>
        </tr>
        </thead>
        <tbody id="tbody">
        {{#posts}}  <!--posts list 를 순회 = java for문-->
            <tr>    <!--list 에서 뽑아낸 객체의 필드-->
                <td>{{id}}</td>
                <td><a href="/posts/update/{{id}}">{{title}}</a></td>
                <td>{{author}}</td>
                <td>{{modifiedDate}}</td>
            </tr>
        {{/posts}}
        </tbody>
    </table>
</div>

{{>layout/footer}}

